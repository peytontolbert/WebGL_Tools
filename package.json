{
  "name": "webgl-map-editor-standalone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5179",
    "build": "vite build",
    "test:chrono": "python3 -m unittest discover -s tests -p \"test_chrono_*.py\" -v",
    "test:chrono:e2e": "node tools/test_chrono_drive_smoke.mjs",
    "test:chrono:full": "npm run test:chrono && npm run test:chrono:e2e",
    "build:resume": "node tools/build_resume_showcase.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 5180",
    "gen:proc-city": "python3 pipelines/run.py proc-city --id ai_city_example --out assets/datasets/generated/ai_city_example --seed ai_city_example --update-manifest",
    "gen:proc-city:tiles": "python3 pipelines/run.py proc-city --id ai_city_example --out assets/datasets/generated/ai_city_example --seed ai_city_example --tile-buildings --update-manifest"
  },
  "dependencies": {
    "colyseus.js": "^0.15",
    "gl-matrix": "^3.4.3",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "playwright": "^1.58.2",
    "sirv": "^2.0.4",
    "vite": "^6.2.3"
  }
}
